package com.ria.process.task;

/**
 * Type of Trigger that leads to a Task being executed.
 *
 * @author abhideep (Abhideep Singh)
 */
public enum TriggerType {
    ON_LOAD,
    ON_CHANGE,
    ON_SUBMIT,
}
